language: bash
sudo: required
dist: xenial
before_install:
 - curl -sL https://deb.nodesource.com/setup_6.x | sudo -E bash -
 - sudo apt-get update -q
 - sudo apt-get install -y ruby2.3-dev nodejs
 - sudo gem install jekyll jekyll-paginate

script: npm test